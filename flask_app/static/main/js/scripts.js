function openVulnerabilityPage(vulnerabilityId) {
    // Redirect to the individual vulnerability page based on the vulnerability ID
    window.location.href = `vulnerabilityPage.html?id=${vulnerabilityId}`;
}